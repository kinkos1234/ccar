# CAR SYSTEM 테스트 시나리오 문서

최종 수정일: 2026-03-19
작성자: 김정현
업데이트: **v2.4.3 아키텍처 전환 반영** — OpenAI/SendGrid/node-cron 관련 테스트 시나리오 제거, n8n 통합 API 테스트 및 n8n 워크플로우 E2E 테스트 시나리오 추가 (2026-03-19)

---

## 1. 테스트 개요

### 1.1 테스트 목적
- CAR 시스템의 모든 기능이 요구사항에 따라 정상 동작하는지 검증
- 권한별 접근 제어가 올바르게 작동하는지 확인
- Date Score 시스템의 정확성 검증
- 다국어 지원 기능의 완전성 확인
- UI 일관성 및 사용자 경험 검증

### 1.2 테스트 환경
- **개발 환경**: localhost:41000 (Frontend), localhost:4000 (Backend)
- **운영 환경**: localhost:41000 (Frontend), localhost:4000 (Backend, PM2: 41100)
- **데이터베이스**: PostgreSQL (db.ccar.internal:5432)
- **브라우저**: Chrome, Firefox, Safari, Edge
- **언어**: 6개 언어 (ko, en, es-mx, hi, vi, zh)

---

## 2. 다국어 지원 테스트 시나리오 (2025-01-04 신규 추가)

### 2.1 언어 전환 기능 테스트

#### 테스트 케이스 2.1.1: 기본 언어 설정
```
전제 조건: 첫 방문 사용자
단계:
1. 시스템에 최초 접속
2. 브라우저 언어 설정 확인
3. 지원 언어인 경우 해당 언어로 표시 확인
4. 미지원 언어인 경우 영어(en)로 표시 확인
예상 결과: 적절한 기본 언어로 시스템 표시
```

#### 테스트 케이스 2.1.2: 언어 전환 기능
```
전제 조건: 로그인된 사용자
단계:
1. 상단 네비게이션의 언어 스위처 클릭
2. 다른 언어 선택 (예: 한국어 → 영어)
3. 페이지 새로고침 대기
4. 모든 텍스트가 선택한 언어로 변경되었는지 확인
5. NEXT_LOCALE cookie에 언어 설정 저장 확인
예상 결과: 모든 UI 요소가 선택한 언어로 즉시 변경
```

#### 테스트 케이스 2.1.3: 언어 설정 지속성
```
전제 조건: 언어를 변경한 사용자
단계:
1. 언어를 변경한 상태에서 브라우저 종료
2. 브라우저 재시작 후 시스템 접속
3. 이전에 선택한 언어로 표시되는지 확인
예상 결과: 이전 언어 설정이 유지됨
```

### 2.2 다국어 UI 요소 테스트

#### 테스트 케이스 2.2.1: Dashboard 다국어 표시
```
전제 조건: 각 언어별로 테스트
단계:
1. Dashboard 페이지 접속
2. KPI 카드 제목 번역 확인
3. 필터 라벨 번역 확인  
4. 차트 제목 및 범례 번역 확인
5. 테이블 헤더 번역 확인
예상 결과: 모든 텍스트가 해당 언어로 정확히 번역
```

#### 테스트 케이스 2.2.2: CAR 관리 다국어 표시
```
전제 조건: 각 언어별로 테스트
단계:
1. CAR 목록 페이지 접속
2. 테이블 헤더 번역 확인
3. 필터 옵션 번역 확인
4. 버튼 텍스트 번역 확인
5. 페이지네이션 텍스트 번역 확인
예상 결과: 모든 UI 요소가 선택한 언어로 표시
```

#### 테스트 케이스 2.2.3: 길이가 긴 언어 UI 최적화
```
전제 조건: 베트남어 또는 스페인어-멕시코 선택
단계:
1. Dashboard 페이지 접속
2. 필터를 ALL 상태로 설정
3. 검색창 텍스트가 뱃지에 가려지지 않는지 확인
4. 모든 버튼과 라벨이 잘리지 않는지 확인
예상 결과: 긴 텍스트에도 UI가 깨지지 않음
```

### 2.3 다국어 오류 메시지 테스트

#### 테스트 케이스 2.3.1: 로그인 오류 메시지
```
전제 조건: 각 언어별로 테스트
단계:
1. 잘못된 로그인 정보 입력
2. 오류 메시지가 선택한 언어로 표시되는지 확인
예상 결과: 언어별 적절한 오류 메시지 표시
```

#### 테스트 케이스 2.3.2: 폼 유효성 검사 메시지
```
전제 조건: CAR 등록/수정 페이지
단계:
1. 필수 필드를 비워둔 채 저장 시도
2. 유효성 검사 메시지가 선택한 언어로 표시되는지 확인
예상 결과: 언어별 유효성 검사 메시지 표시
```

---

## 3. Date Score 시스템 테스트 시나리오

### 1.1 Date Score 계산 테스트
```javascript
describe('Date Score Calculation', () => {
  test('기한 내 완료 시 +5점', () => {
    const dueDate = '2024-12-15';
    const completionDate = '2024-12-15';
    expect(calculateDateScore(dueDate, completionDate)).toBe(5);
  });
  
  test('1일 지연 시 +3점', () => {
    const dueDate = '2024-12-15';
    const completionDate = '2024-12-16';
    expect(calculateDateScore(dueDate, completionDate)).toBe(3);
  });
  
  test('30일 초과 지연 시 -5점', () => {
    const dueDate = '2024-12-15';
    const completionDate = '2025-01-20';
    expect(calculateDateScore(dueDate, completionDate)).toBe(-5);
  });
  
  test('CONTINUOUS 이벤트가 아닌 경우 null', () => {
    const car = { eventType: 'ONE_TIME' };
    expect(getDateScore(car)).toBeNull();
  });
});
```

### 1.2 Date Score API 통합 테스트
| 테스트 케이스 | 입력 | 기대 결과 | 상태 |
|-------------|------|----------|------|
| CONTINUOUS 이벤트 조회 | eventType: "CONTINUOUS" | dateScore 필드 포함 | ✅ 통과 |
| ONE_TIME 이벤트 조회 | eventType: "ONE_TIME" | dateScore: null | ✅ 통과 |
| 날짜 없는 경우 | dueDate: null | dateScore: null | ✅ 통과 |
| 대시보드 통계 조회 | /api/dashboard/stats | avgDateScore 포함 | ✅ 통과 |

---

## 4. UI 통일성 테스트 시나리오

### 2.1 모달 레이아웃 통일성 테스트
| 테스트 항목 | Dashboard 모달 | CAR 상세 모달 | 개별 페이지 | 상태 |
|------------|---------------|--------------|------------|------|
| 헤더 구조 | 제목 + 닫기 버튼 | 제목 + 닫기 버튼 | 동일 | ✅ 통과 |
| 기본 정보 섹션 | 2열 그리드 | 2열 그리드 | 2열 그리드 | ✅ 통과 |
| 텍스트 섹션 | 세로 배치 | 세로 배치 | 세로 배치 | ✅ 통과 |
| 점수 섹션 | Event Type별 분기 | Event Type별 분기 | Event Type별 분기 | ✅ 통과 |
| 버튼 섹션 | 우측 정렬 | 우측 정렬 | 우측 정렬 | ✅ 통과 |

### 2.2 Event Type별 점수 표시 분기 테스트
```javascript
describe('Event Type Score Display', () => {
  test('ONE_TIME: 1열 그리드 (주관적 점수만)', () => {
    const car = { eventType: 'ONE_TIME', subjectiveScore: 4.5 };
    render(<ScoreSection car={car} />);
    
    const grid = screen.getByTestId('score-grid');
    expect(grid).toHaveClass('max-w-xs'); // 1열 제약
    expect(screen.getByText('주관적 점수')).toBeInTheDocument();
    expect(screen.queryByText('Date Score')).not.toBeInTheDocument();
  });
  
  test('CONTINUOUS: 3열 그리드 (내부+고객+Date Score)', () => {
    const car = { 
      eventType: 'CONTINUOUS', 
      internalScore: 4.0,
      customerScore: 3.5,
      dateScore: 2
    };
    render(<ScoreSection car={car} />);
    
    const grid = screen.getByTestId('score-grid');
    expect(grid).toHaveClass('grid-cols-3');
    expect(screen.getByText('내부 점수')).toBeInTheDocument();
    expect(screen.getByText('고객 점수')).toBeInTheDocument();
    expect(screen.getByText('Date Score')).toBeInTheDocument();
  });
  
  test('기타 Type: 4열 그리드 (모든 점수)', () => {
    const car = { 
      eventType: 'CUSTOM',
      internalScore: 4.0,
      customerScore: 3.5, 
      subjectiveScore: 4.5,
      dateScore: 1
    };
    render(<ScoreSection car={car} />);
    
    const grid = screen.getByTestId('score-grid');
    expect(grid).toHaveClass('grid-cols-4');
    expect(screen.getByText('내부 점수')).toBeInTheDocument();
    expect(screen.getByText('고객 점수')).toBeInTheDocument();
    expect(screen.getByText('주관적 점수')).toBeInTheDocument();
    expect(screen.getByText('Date Score')).toBeInTheDocument();
  });
});
```

---

## 5. 기본 기능 테스트

### 3.1 대시보드 기능 테스트
| 기능 | 테스트 케이스 | 상태 |
|------|-------------|------|
| KPI 카드 | 총 이벤트 수, 평균 점수, 진행중 이벤트, 완료율 표시 | ✅ 통과 |
| 차트 시각화 | PieChart, BarChart, LineChart 정상 렌더링 | ✅ 통과 |
| 필터링 | Corporation, Customer, Department 필터 작동 | ✅ 통과 |
| 페이지네이션 | 15개 항목/페이지, 자동 숨김/표시 | ✅ 통과 |
| 정렬 기능 | 날짜, 고객사, 부서, Score 정렬 | ✅ 통과 |

### 3.2 AI 분석 기능 테스트 (v2.4.3 — Ollama 기반)
| 기능 | 테스트 케이스 | 상태 |
|------|-------------|------|
| 이슈 요약 생성 | Ollama(로컬 LLM) 기반 요약 생성 | ✅ 통과 |
| 전략 제언 생성 | Ollama(로컬 LLM) 기반 5개 항목 구조화 | ✅ 통과 |
| Evidence 생성 | 전체 이력 기반 근거 자료 | ✅ 통과 |
| Fallback 처리 | AI 응답 실패 시 안전한 대체 | ✅ 통과 |

### 3.3 n8n 통합 API 테스트 (v2.4.3 신규)
| 기능 | 테스트 케이스 | 기대 결과 | 상태 |
|------|-------------|----------|------|
| POST /api/n8n/report/save | 보고서 저장 정상 동작 | 200 OK, 보고서 DB 저장 확인 | ✅ 통과 |
| GET /api/n8n/health | 헬스체크 응답 | 200 OK, `{ status: "ok" }` 반환 | ✅ 통과 |
| X-N8N-Webhook-Secret 인증 (유효하지 않은 키) | 잘못된 시크릿 키로 요청 | 401 Unauthorized 거부 | ✅ 통과 |
| X-N8N-Webhook-Secret 인증 (키 누락) | 헤더 없이 요청 | 401 Unauthorized 거부 | ✅ 통과 |

### 3.4 n8n 워크플로우 E2E 테스트 (v2.4.3 신규)
```
전제 조건: n8n 워크플로우가 활성화된 상태, Ollama 서비스 실행 중
단계:
1. n8n Schedule Trigger 수동 실행
2. SQL 쿼리 노드가 PostgreSQL에서 CAR 데이터 정상 추출 확인
3. Ollama AI 분석 노드가 추출된 데이터를 기반으로 분석 결과 생성 확인
4. 보고서 저장 노드가 POST /api/n8n/report/save를 통해 분석 보고서 저장 확인
5. 이메일 발송 노드가 분석 보고서를 수신자에게 정상 발송 확인
예상 결과: 전체 워크플로우(Schedule Trigger → SQL 쿼리 → Ollama AI 분석 → 보고서 저장 → 이메일 발송)가 정상 완료
```

---
## 6. 다국어(i18n) 테스트 시나리오

### 4.1 언어 변경 UI 테스트

| 테스트 항목 | 기대 결과 | 상태 |
|-------------|-----------|------|
| 언어 드롭다운 존재 | 네비게이션 우측 상단에 위치 | ✅ 통과 |
| 언어 선택 후 텍스트 변경 | 전체 UI 텍스트가 즉시 선택 언어로 변경됨 | ✅ 통과 |
| 선택 언어 유지 | 페이지 새로고침 후에도 언어 상태 유지 (`NEXT_LOCALE cookie`) | ✅ 통과 |

### 4.2 UI 번역 적용 테스트

| 영역 | 기대 동작 | 상태 |
|------|-----------|------|
| KPI 카드 제목 | 각 언어로 자연스럽게 렌더링됨 (예: "총 이벤트 수" → "Total Events") | ✅ 통과 |
| 테이블 헤더 | 정렬 아이콘 포함한 텍스트가 다국어로 출력 | ✅ 통과 |
| 차트 라벨/툴팁 | 날짜/카테고리 라벨, 범례가 언어별로 자연스럽게 출력 | ✅ 통과 |
| Date Score 툴팁 | 언어별 점수 설명 표시 (예: "기한 초과 30일 이상" → 해당 언어로 번역) | ✅ 통과 |

### 4.3 fallback 및 누락 테스트

- 번역 JSON 파일에 키가 누락된 경우 기본 언어(`en`)로 자동 출력
- 모든 주요 라벨(`button.submit`, `filter.customer`, `score.date`)이 누락 없이 표시되는지 검증

### 4.4 언어 선택 라우팅 테스트

- `?lang=zh`로 접근 시 자동으로 중국어 텍스트 출력됨
- 언어 선택 후 URL 파라미터가 업데이트됨 (`/dashboard?lang=es-mx`)
- `app/[lang]/` 기반 라우팅이 정상 작동하는지 확인 (Next.js 기반 구조일 경우)

---

## 7. 점수 정합성 테스트 (2025-07-02)

### 5.1 전체 데이터 검증 테스트
```javascript
describe('Score Recalculation Verification', () => {
  test('전체 343개 CAR 데이터 점수 검증', async () => {
    const cars = await getAllCars();
    expect(cars.length).toBe(343);
    
    for (const car of cars) {
      // Total Score 검증
      const expectedScore = calculateTotalScore(car);
      expect(Math.abs(car.score - expectedScore)).toBeLessThan(0.1);
      
      // Date Score 검증 (CONTINUOUS만)
      if (car.eventType === 'CONTINUOUS' && car.dueDate && car.completionDate) {
        const expectedDateScore = calculateDateScore(car.dueDate, car.completionDate);
        expect(car.dateScore).toBe(expectedDateScore);
      }
    }
  });
});
```

### 5.2 BigInt 직렬화 테스트
```javascript
describe('BigInt Serialization', () => {
  test('모든 API 응답에서 BigInt 안전 처리', async () => {
    const response = await fetch('/api/car/1');
    const data = await response.json();
    
    // JSON 파싱이 성공해야 함 (BigInt 오류 없음)
    expect(data).toBeDefined();
    expect(typeof data.id).toBe('string'); // BigInt → string 변환
  });
});
```

---

## 8. 성능 테스트

### 6.1 API 응답 시간 테스트
| API 엔드포인트 | 목표 응답 시간 | 실제 응답 시간 | 상태 |
|---------------|---------------|---------------|------|
| GET /api/car | < 500ms | ~200ms | ✅ 통과 |
| GET /api/car/:id | < 300ms | ~150ms | ✅ 통과 |
| GET /api/dashboard/stats | < 800ms | ~400ms | ✅ 통과 |
| POST /api/ai/analyze | < 20s | ~12s | ✅ 통과 |

### 6.2 Date Score 계산 성능 테스트
```javascript
describe('Date Score Performance', () => {
  test('100개 CAR의 Date Score 일괄 계산 < 100ms', async () => {
    const cars = await getCars(100);
    const startTime = Date.now();
    
    cars.forEach(car => {
      if (car.eventType === 'CONTINUOUS') {
        calculateDateScore(car.dueDate, car.completionDate);
      }
    });
    
    const duration = Date.now() - startTime;
    expect(duration).toBeLessThan(100);
  });
});
```

---

## 9. AI 분석 Date Score 통합 테스트 (2025-07-02)

### 7.1 AI 분석에 Date Score 포함 테스트
```javascript
describe('AI Analysis with Date Score', () => {
  test('Evidence에 시간 관리 정보 포함', async () => {
    const customer = 'TESLA';
    const analysis = await generateAIAnalysis(customer);
    
    expect(analysis.evidence).toContain('시간 관리');
    expect(analysis.evidence).toContain('기한준수');
    expect(analysis.evidence).toContain('지연');
  });
  
  test('전략 제언에 Date Score 개선 방안 포함', async () => {
    const analysis = await generateAIAnalysis('TESLA');
    
    expect(analysis.aiRecommendation).toContain('조치');
    expect(analysis.aiRecommendation).toContain('예상 효과');
    // Date Score 관련 키워드 확인
    const hasTimeKeywords = /시간|지연|기한|일정|Date Score/i.test(analysis.aiRecommendation);
    expect(hasTimeKeywords).toBe(true);
  });
});
```

---

## 10. 브라우저 호환성 테스트

### 8.1 지원 브라우저
| 브라우저 | 버전 | 기본 기능 | Date Score | UI 통일성 | 상태 |
|---------|------|----------|-----------|-----------|------|
| Chrome | 120+ | ✅ | ✅ | ✅ | 완전 지원 |
| Firefox | 115+ | ✅ | ✅ | ✅ | 완전 지원 |
| Safari | 16+ | ✅ | ✅ | ✅ | 완전 지원 |
| Edge | 120+ | ✅ | ✅ | ✅ | 완전 지원 |

---

## 11. 🎉 테스트 완료 현황

### 9.1 전체 테스트 통과율
- ✅ **기본 기능 테스트**: 100% 통과 (45/45)
- ✅ **Date Score 테스트**: 100% 통과 (15/15)
- ✅ **UI 통일성 테스트**: 100% 통과 (12/12)
- ✅ **성능 테스트**: 100% 통과 (8/8)
- ✅ **보안 테스트**: 100% 통과 (10/10)
- ✅ **접근성 테스트**: 100% 통과 (6/6)
- ✅ **Risk Mitigation 테스트**: 100% 통과 (12/12)
- ✅ **n8n 통합 테스트**: 100% 통과 (5/5)

### 9.2 핵심 품질 지표
- **API 평균 응답 시간**: 280ms (목표: 500ms 이내)
- **Date Score 계산 정확도**: 100% (343/343 검증 완료)
- **UI 일관성**: 100% (모든 모달 레이아웃 통일)
- **브라우저 호환성**: 100% (Chrome, Firefox, Safari, Edge)
- **접근성 준수율**: 100% (WCAG 2.1 AA 기준)

---

## 10. Risk Mitigation 테스트 시나리오 (v2.4.3)

### 10.1 Risk Checkbox 기능 테스트
```
전제 조건: CAR 등록/수정 페이지
단계:
1. CAR 등록 폼에서 Risk Mitigation 체크박스 확인
2. 체크박스 선택 시 리스크 레벨 선택기와 설명 입력 필드 표시 확인
3. 리스크 레벨 선택 (LOW/MEDIUM/HIGH/CRITICAL)
4. 리스크 설명 입력
5. CAR 저장
6. 저장된 CAR에서 리스크 정보 정상 표시 확인
예상 결과: 리스크 정보가 정상적으로 저장 및 표시됨
```

### 10.2 Risk Indicator 표시 테스트
| 테스트 케이스 | 입력 | 기대 결과 | 상태 |
|-------------|------|----------|------|
| 리스크 없는 CAR | riskMitigation: false | 인디케이터 미표시 | ✅ 통과 |
| LOW 리스크 CAR | riskLevel: "LOW" | 오렌지색 인디케이터 | ✅ 통과 |
| MEDIUM 리스크 CAR | riskLevel: "MEDIUM" | 노란색 인디케이터 | ✅ 통과 |
| HIGH 리스크 CAR | riskLevel: "HIGH" | 빨간색 인디케이터 | ✅ 통과 |
| CRITICAL 리스크 CAR | riskLevel: "CRITICAL" | 진한 빨간색 인디케이터 | ✅ 통과 |

### 10.3 Risk Analytics API 테스트
```javascript
describe('Risk Analytics API', () => {
  test('리스크 요약 통계 조회', async () => {
    const response = await fetch('/api/car/risk-analytics');
    const data = await response.json();
    expect(data.success).toBe(true);
    expect(data.data.summary).toHaveProperty('totalRiskEvents');
    expect(data.data.summary).toHaveProperty('completedRiskEvents');
    expect(data.data.summary).toHaveProperty('completionRate');
    expect(data.data.summary).toHaveProperty('avgScore');
  });

  test('카테고리별 리스크 분석', async () => {
    const response = await fetch('/api/car/risk-analytics');
    const data = await response.json();
    expect(Array.isArray(data.data.byCategory)).toBe(true);
    data.data.byCategory.forEach(item => {
      expect(item).toHaveProperty('category');
      expect(item).toHaveProperty('count');
      expect(item).toHaveProperty('avgScore');
    });
  });
});
```

### 10.4 Risk 필터링 테스트
| 테스트 케이스 | 필터 | 기대 결과 | 상태 |
|-------------|------|----------|------|
| 리스크 있는 CAR만 | risk=true | riskMitigation=true인 CAR만 표시 | ✅ 통과 |
| 리스크 없는 CAR만 | risk=false | riskMitigation=false인 CAR만 표시 | ✅ 통과 |
| 전체 CAR | risk=all | 모든 CAR 표시 | ✅ 통과 |
| HIGH 레벨만 | riskLevel=HIGH | riskLevel="HIGH"인 CAR만 표시 | ✅ 통과 |

### 10.5 Risk Dashboard (Gantt 차트) 테스트
| 테스트 케이스 | 기대 결과 | 상태 |
|-------------|----------|------|
| 스코어보드 렌더링 | 법인별/카테고리별 매트릭스 표시 | ✅ 통과 |
| Gantt 차트 타임라인 | ±6개월 범위 정상 표시 | ✅ 통과 |
| 진행률 색상 | CLOSED=초록, IN_PROGRESS=노랑, DELAYED=빨강 | ✅ 통과 |
| 법인 섹션 접기/펼치기 | 클릭 시 정상 동작 | ✅ 통과 |
| CAR 클릭 시 상세 모달 | 모달 정상 오픈 | ✅ 통과 |

---
